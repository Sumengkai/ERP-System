import { Injectable } from '@angular/core';
import { Observable, of } from 'rxjs';

export interface PurchaseFace {
    id: number;
    name: string;
  }
  export interface PurchaseFace2 {
    randomId: Number;
    req1: string;
    req2: string;
    req3: string;
    req4: string;
  }